name: Debug Build V3

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
    - name: Kodu Çek
      uses: actions/checkout@v4

    - name: .NET Kurulumu
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'

    # --- İŞTE AJAN KOD BURASI ---
    - name: 📂 DOSYA YAPISINI GÖSTER (DEBUG)
      run: |
        echo "--- ANA DİZİN ---"
        ls -R
        echo "-----------------"

    # --- CLIENT (WPF) ---
    - name: Client (WPF) Derle
      # Burası muhtemelen çalışıyor, dokunmadık
      run: dotnet publish JsonABC/JsonABC/JsonABC.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o ./publish/client

    - name: Client Ziple
      run: Compress-Archive -Path ./publish/client/* -DestinationPath ./HizYarisi_Client.zip

    # --- SERVER (API) ---
    - name: Server (API) Derle
      # Hata veren yer burası. Yukarıdaki LS komutu sayesinde doğru yolu bulacağız.
      # Şimdilik eski yolu bırakıyorum, hata verse bile logları göreceğiz.
      continue-on-error: true 
      run: dotnet publish JsonABC/JsonServer/JsonServer.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o ./publish/server

    - name: Server Ziple (Hata varsa atla)
      if: success()
      run: Compress-Archive -Path ./publish/server/* -DestinationPath ./HizYarisi_Server.zip

    # --- RELEASE ---
    - name: Release Oluştur
      if: success()
      uses: softprops/action-gh-release@v2
      with:
        files: |
          HizYarisi_Client.zip
          HizYarisi_Server.zip
        tag_name: ${{ github.ref_name }}
        name: Debug Sürümü ${{ github.ref_name }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}